Adicione o arquivo de música aqui chamado `cidade_vizinha.mp3` ou use a função `setAudio(url)` no console para apontar para uma URL externa (ex.: SoundCloud/MP3 direto).

Observação: por questões de direitos autorais, assegure-se de ter permissão para usar a música antes de publicar o site.